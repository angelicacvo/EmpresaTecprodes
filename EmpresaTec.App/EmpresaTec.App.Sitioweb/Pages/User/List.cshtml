@page
@model EmpresaTec.App.Sitioweb.App.Pages.ListModelU
@{
}
<h1>Listado de usuarios</h1>
<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Id de usuario</th>
            <th>Número telefonico</th>
            <th>Dirección</th>
            <th>Id del proyecto</th>

            <th>Borrar</th>
            <th>Editar</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.users) //Model conecta con el List.cshtml.cs
        {
         <tr>
            <td>@user.name</td>
            <td>@user.IdNumber</td>
            <td>@user.phoneNumber</td>
            <td>@user.address</td>
            <td>@user.projectId</td>
            <td>
                <a class="btn btn-danger" asp-page=".\Delete" asp-route-id="@user.userId">Borrar</a>
            </td>
            <td>
                <a class="btn btn-info" asp-page=".\Edit" asp-route-id="@user.userId">Editar</a>
            </td>
        </tr>
        }
    </tbody>
</table>

<td>
    <a class="btn btn-warning" asp-page=".\Create">Crear usuario</a>
</td>